apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "altinity-clickhouse-operator.fullname" . }}-test-connection"
  labels:
    {{- include "altinity-clickhouse-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "altinity-clickhouse-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
